import structures.matrices.DenseMatrix;
import structures.matrices.ProfileMatrix;
import structures.matrices.Vector;

public class Gauss {
    public static <T extends Number> Vector<T> evaluate(DenseMatrix<T> matrix) {
        return null;
    }

    public static <T extends Number> Vector<T> evaluate(ProfileMatrix<T> matrix) {
        return null;
    }
}
